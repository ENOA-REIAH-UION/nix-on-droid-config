# nix-on-droid-config

我的 Nix-on-Droid 配置文件，用于在 Android 终端中构建一致的开发环境。

## 快速开始

1.  **克隆此仓库到 `~/.config/nix-on-droid`**:
    ```bash
    git clone https://github.com/ENOA-REIAH-UION/nix-on-droid-config ~/.config/nix-on-droid
